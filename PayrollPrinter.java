package paychecks;

import java.util.ArrayList;
import java.util.List;

/**
 * This class contains only one static method that is used to print out salary
 * report.
 * 
 * @author Devdutt Santhosh
 *
 */
public class PayrollPrinter{
	/**
	 * prints to stdout a salary report for the given Payroll object as specified by
	 * the SRS supplied elsewhere.
	 * 
	 * @param p This is an object from the Payroll class
	 */
	public static void printSalaryReport(Payroll p){
//		List<Employee> abc = new ArrayList<Employee>();
//		abc = p.getEmployees();
//		for (int i = 0; i < abc.size(); i++){
//			Employee emp = abc.get(i);
//			int totalsal = emp.getSalary() + emp.getBonus();
//			System.out.println(emp.getName() + ", " + emp.getTitle() + ": $" + totalsal);
//		}
//		System.out.println("Total Salaries: $" + p.getTotalSalaries());
	}
}
